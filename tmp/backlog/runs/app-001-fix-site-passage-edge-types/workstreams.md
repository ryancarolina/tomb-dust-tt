# Workstreams — APP-001

| Stream | Scope | Files | Depends |
|--------|-------|-------|---------|
| WS1 | Site JSON remap + spec sync | `build/data/sites/*.json`, `tmp/app-exploration-delve-spec.md` | — |

Single stream — no parallel subagents (trivial data edit).
