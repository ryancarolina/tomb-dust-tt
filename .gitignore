# Play runtime (campaign DB, active session, TTS cache)
play/workspace/.local/*
!play/workspace/.local/.gitkeep
play/workspace/config.yaml
play/workspace/*.db
play/play/

# App secrets
app/.env

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# App runtime
app/logs/
app/session_state.json
test-scene.txt

# tmp/ — app development specs (app-*-spec.md); do not add unrelated scratch here
tmp/.active-ticket.json
tmp/.active-batch.json

# Agent mistake — dev-team artifacts belong in tmp/backlog/runs/
.dev-team/
